@model IEnumerable<ValeDoSolFinal_1.Models.Leitura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lote.CPF)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataLeitura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumeroLeitura)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lote.CPF)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataLeitura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroLeitura)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
